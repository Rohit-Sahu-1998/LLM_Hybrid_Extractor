# LLM_Hybrid_Extractor
Hybrid Document AI system that extracts structured data from invoices and medical forms using PaddleOCR (text + bounding boxes) and Qwen (semantic extraction). It aligns values with document locations, shows OCR &amp; AI confidence, validates ICD/CPT codes, and enables review with highlighting in a Streamlit UI.
